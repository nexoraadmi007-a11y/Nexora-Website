'use client'
import Link from 'next/link'
import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'
import { Card } from '@/components/ui'
export default function GrowthLoginPage(){const router=useRouter();const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage('Signing in…');const response=await fetch('/api/growth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget).entries()))});const body=await response.json().catch(()=>({}));if(!response.ok){setMessage(body.message||'Sign in failed.');setBusy(false);return}router.replace('/growth/portal');router.refresh()}return <main className="associate-login"><Card><Link className="brand" href="/">Nexora Institute</Link><p className="eyebrow">Growth Associate Portal</p><h1>Sign in</h1><form className="form-grid" onSubmit={submit}><label className="field"><span>WhatsApp Number</span><input name="whatsapp" required inputMode="tel" autoComplete="tel"/></label><label className="field"><span>Password</span><input name="password" type="password" required autoComplete="current-password"/></label>{message?<p className="form-message" aria-live="polite">{message}</p>:null}<button className="btn btn-primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><p>New Growth Associate? <Link href="/growth/register">Create an account</Link></p><p className="muted">Password recovery will be available when verified WhatsApp messaging is enabled.</p></Card></main>}
