import { notFound } from 'next/navigation'
import { AppShell } from '@/components/shell'
import { Card } from '@/components/ui'
import { findProgramme, formatNaira } from '@/config/programmes'
export default async function CoursePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const course = findProgramme(slug); if (!course) notFound(); return <AppShell title={course.name}><div className="page-grid"><div className="dashboard-grid"><Card><p className="eyebrow">Course Overview</p><h3>{course.proposition}</h3><p className="muted">{course.duration}</p><ul className="list">{course.outcomes.map((outcome) => <li key={outcome}>{outcome}</li>)}</ul></Card><Card><p className="eyebrow">Course Price</p><p className="price">{formatNaira(course.priceNgn)}</p><p className="muted">This independent course has its own classes and assignments.</p><a className="btn btn-primary" href={`/checkout?course=${course.slug}`}>Select Course</a></Card></div><div className="grid-2"><Card><h3>Classes</h3><p className="muted">Open your published meeting schedule and join links.</p><a className="btn btn-secondary" href="/app/classes">View Classes</a></Card><Card><h3>Assignments</h3><p className="muted">Review course tasks and submit your work.</p><a className="btn btn-secondary" href="/app/assignments">View Assignments</a></Card></div></div></AppShell> }
